# Optional image API keys (see src/utils/imageConfig.js)
# When set, product images are fetched from that API with proper auth.
# Without them the site uses the built-in public-CDN images automatically.
VITE_PEXELS_API_KEY=
VITE_UNSPLASH_ACCESS_KEY=